# Host: Cursor

- **Entry:** Repo root `AGENTS.md` is injected into agent context.
- **Rules:** `.cursor/rules/overkill-*.mdc` reinforce boot order.
- **Paths:** Use workspace-relative paths; global brain at `~/.overkill/`.
- **MCP:** Use only what the workspace exposes; keep secrets out of markdown.
