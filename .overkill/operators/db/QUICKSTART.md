# DB operator quickstart

- Department: `DB`
- Focus: schema design, migrations, constraints, indexes, RLS/data integrity, query performance.
- Required companions: API contract references in backend specs and migration evidence in diagnostics.

## Department gates (minimum)
- Migration has rollback notes.
- Data integrity checks defined (constraints / invariants).
- Security posture for data access defined (RLS/policies where relevant).
